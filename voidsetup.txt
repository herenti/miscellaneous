fdisk /dev/nvme0n1
cryptsetup luksFormat --type luks1 /dev/nvme0n1p2
cryptsetup luksOpen /dev/nvme0n1p2 voidmage
vgcreate voidmage /dev/mapper/voidmage
lvcreate --name swap -L 8G voidmage
lvcreate --name root -l 100%FREE voidmage
mkfs.ext4 -L root /dev/voidmage/root
mkswap /dev/voidmage/swap
swapon /dev/voidmage/swap
mount /dev/voidmage/root /mnt
mkfs.vfat /dev/nvme0n1p1
mkdir -p /mnt/boot/efi
mount /dev/nvme0n1p1 /mnt/boot/efi
mkdir -p /mnt/var/db/xbps/keys
cp /var/db/xbps/keys/* /mnt/var/db/xbps/keys/
xbps-install -Sy -R https://repo-default.voidlinux.org/current -r /mnt base-system cryptsetup grub-x86_64-efi lvm2 nano void-repo-nonfree
xgenfstab /mnt > /mnt/etc/fstab
xchroot /mnt
chown root:root /
chmod 755 /
passwd root
echo voidmage > /etc/hostname
echo "LANG=en_US.UTF-8" > /etc/locale.conf
echo "en_US.UTF-8 UTF-8" >> /etc/default/libc-locales
xbps-reconfigure -f glibc-locales
blkid
nano /etc/default/grub
GRUB_ENABLE_CRYPTODISK=y
rd.lvm.vg=voidmage rd.luks.uuid=<UUID>
grub-install --target=x86_64-efi --efi-directory=/boot/efi
xbps-install -Sy
xbps-install intel-ucode
xbps-reconfigure -fa
useradd herenti
passwd herenti
usermod -a -G wheel herenti
EDITOR=nano visudo
xbps-install xorg xinit intel-media-driver kde-plasma sddm dolphin konsole firefox ocean-sound-theme elogind libspa-bluetooth bluez ufw NetworkManager wine NetworkManager-openvpn openvpn network-manager-applet kate mpv
ln -s /etc/sv/dbus /etc/runit/runsvdir/default/
ln -s /etc/sv/NetworkManager /etc/runit/runsvdir/default/
ln -s /etc/sv/sddm /etc/runit/runsvdir/default/
ln -s /etc/sv/elogind /etc/runit/runsvdir/default/
mkdir -p /etc/pipewire/pipewire.conf.d
ln -s /usr/share/examples/wireplumber/10-wireplumber.conf /etc/pipewire/pipewire.conf.d/
mkdir -p /etc/pipewire/pipewire.conf.d
ln -s /usr/share/examples/pipewire/20-pipewire-pulse.conf /etc/pipewire/pipewire.conf.d/
ln -s /etc/sv/bluetoothd /etc/runit/runsvdir/default/
ln -s /etc/sv/ufw /etc/runit/runsvdir/default/
usermod -a -G bluetooth herenti
reboot
ufw enable
if needed (rm /etc/resolv.conf)
sudo resolvconf -u

xbps-install nvidia580 nvidia580-libs-32bit mesa-dri-32bit steam libgcc-32bit libstdc++-32bit libdrm-32bit libglvnd-32bit



